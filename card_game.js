// ========== CARD_GAME Placeholder ==========
function playCardGame() {
  // Placeholder for Jace's game
  background(40, 10, 70);
  fill(255);
  textAlign(CENTER, CENTER);
  textSize(24);
  text("Card Game Placeholder", width / 2, height / 2);
}
